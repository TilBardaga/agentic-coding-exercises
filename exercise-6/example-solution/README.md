# Example solution (optional)

- **`execute.example.md`** — Sample body for an **`/execute`** command (adapt paths, validation policy, and task-parsing hints to your plan template).

Learners implement their own command in **`commands/`** (or equivalent); this file is a reference, not a unique correct answer.
